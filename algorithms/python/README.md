## Tests
### Pre-requisites:
- python v2.7
- python-pip v8.1.2

#### install dependencies
```shell
pip install -r requirements-test.txt
```

### run the test suite
```shell
$ PYTHONPATH=$(pwd) ivoire -v spec/
```
